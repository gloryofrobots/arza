
var (x, y, z) = import("x.y.z")

func add3(x, y, z) {
    return x + y + z
}

type T {
    x = 1
    y = 2
    z = 3
    var(n, z, x)
    n = nil
    z = nil
    x = nil
}


(fun trait_x(T1)
    (def T1.add3 func (x, y, z)
        return x + y + z
    )
)

fun trait_x(T1) {
    def T1.add3 func (x, y, z){
        return x + y + z
    }
}
