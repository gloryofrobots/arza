fun map(seq, fn) =
    cond seq
        if [] then seq.empty()
        if [0] = x

cond X
     if [0] == 1, [1] == #c, [1..] == [1,2,3] then
     if #ccc then
     if of Map then
     if [0] of Map then
     if [#ccc] as c of Int and c == 1 then
     if [#ccc] of Int == 1 then



