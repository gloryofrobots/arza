load seq
load _lists

generic seq
generic first
generic rest
generic is_empty
generic empty


specify seq.seq
    case (l of List) -> l end

specify seq.first case(l of List) = _lists.head
    case (l of List) -> l end
