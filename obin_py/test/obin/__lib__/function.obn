import derive

extend Function
    use derive:TStr
    use derive:TRepr
    use derive:TEq

extend Method
    use derive:TStr
    use derive:TRepr
    use derive:TEq

extend Partial
    use derive:TStr
    use derive:TRepr
    use derive:TEq